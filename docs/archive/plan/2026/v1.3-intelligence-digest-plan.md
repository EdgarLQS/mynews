---
title: mynews v1.3 情报简报计划
doc_type: plan
status: archived
implementation_status: implemented
version: 1.3
created: 2026-08-09
updated: 2026-08-09
owner: project-maintainers
superseded_by: v1.4-source-plugins-plan.md
---

# mynews v1.3 情报简报计划

## 基线与边界

本计划从 `origin/main@768403924666c3e17a941b903910743f685f7fbc` 创建，分支为 `codex/v1.3-intelligence-digest`。v1.3 只读取已经保存的 RunReport 和上一期 Digest，新增独立的整理、聚合、排序、摘要和导出层；不修改来源采集、相关性过滤、`verified` 定义、第一方证据解析、pending 重试或现有 Store 事务门槛。

`verified` 仍只能来自程序核验的第一方证据。`unverified` 只能进入“线索观察”，必须显示核验原因和重试状态；任何线索文本、模型输出或摘要都不能把条目升级为事实。

## 交付范围

1. 独立 `DigestBuilder`：输入 `RunReport` 与可选上一期 `Digest`，保守聚合同一事件，按相关性 35%、热度 25%、时效 20%、事件类型 20% 确定性排序，并标记 `new`、`updated`、`ongoing`。
2. 独立 Digest Schema 1.0：主榜只包含 `verified`；线索观察只包含 `unverified`，保留原因、重试和证据引用。
3. Codex 摘要 seam：只把 RunReport 中已保存的第一方证据传给模型，严格校验返回的条目 ID、证据 URL 引用和中文摘要/影响判断；模型异常、超时、非法 JSON 或不合法引用均回退到标题与已保存证据摘录，整体状态为 `partial`。核验和摘要分别支持 `low/medium/high/xhigh/max` 推理强度，默认 `medium`；该运行时配置不改变证据门槛。
4. 原子输出：在 `--out-dir` 下生成 `digests/<digest-id>.json`、`digest-latest.json` 和 `digest-latest.md`；历史文件与 latest 一起提交，失败恢复旧 latest，不留下临时文件。
5. 中文 CLI 参数：`collect --verification-reasoning-effort` 和 `digest --summary-reasoning-effort` 均可选，默认 `medium`；Digest Schema 1.0 不持久化摘要推理强度。`mynews digest` 还支持 `--run`、`--out-dir`、`--max-items`、`--summary-model`、`--summary-timeout`、`--no-codex`。个人使用可以关闭 Codex 后人工打开线索链接；该模式不升级 `verified`，也不替代 G6-V。`scripts/collect.sh` 仅在显式 `--digest` 时追加简报生成，默认采集行为不变。
6. 同步系统架构、Digest 数据契约、功能矩阵、根 README、计划索引和 ADR。

## 聚合、排序与生命周期口径

- 精确 URL 或相同事件键可聚合；不同 URL 还必须同时满足相同事件类型、存在共同实体、标题高相似度和合理日期距离。仅同厂商、同类型或相似关键词不得合并。
- 时效分为 0–100，按报告完成时刻与事件时间的天数衰减；事件类型权重固定记录在数据契约中。排序使用整数化权重并以事件键作最终稳定 tie-breaker。
- 当前事件首次出现在上一期之后为 `new`；事实、证据、标题或评分发生变化为 `updated`；其余为 `ongoing`。上一期同时检查主榜和线索观察。

## v1.2 真实验收回写

以下事实来自已归档的 [v1.2 真实环境验收与交接清单](../../testing/2026/v1.2-real-environment-acceptance.md)，不把未完成门禁改写为 Verified：

- 执行日期为 2026-08-09，时区 Asia/Shanghai；隔离目录运行，未调用真实 launchd，也未写入仓库 `output/`、`state/`、`logs/`。
- G6-S 实际命令为 `mynews probe`，退出码 0、状态 `complete`；`zhihu-hot/blocked(http_403)` 和 `bloomberg-ai/blocked(public_metadata_unavailable)` 如实保留，其他记录见归档清单。
- G6-V/G7 实际使用两轮同一七天窗口（2026-08-02 至 2026-08-08）、模型 `gpt-5.6-luna`，每轮执行 `mynews collect`、`mynews validate --check-evidence` 和 `mynews report`。Run ID 为 `2026-08-09T00:46:16.890957+00:00`、`2026-08-09T00:55:18.922870+00:00`。
- 首轮 `complete`：candidate 19、filtered 14、normalized 5、item 5、attempted 5、pending 3、verified 0；次轮 `complete`：candidate 15、filtered 11、normalized 4、item 3、deduplicated 4、retried 3、pending 2、verified 0。两轮 validate/report 均退出 0，历史 run、latest、dedup、pending 和临时文件检查通过。
- G6-V 证据为：没有任何 `verification_reason=codex_primary_evidence` 且通过完整程序校验的 discovery 条目；因此没有可回写的第一方 evidence URL、发布日期、逐字摘录或正文哈希样本，结论是 `BLOCKED`，不是 `Verified`。

## 离线验收门槛

| 门禁 | 要求 |
| --- | --- |
| D1 | 聚类、误合并、排序权重、new/updated/ongoing 生命周期和 max-items 测试通过 |
| D2 | 主榜/线索隔离、原因/重试展示和无证据不升级测试通过 |
| D3 | Codex 合法输出、未知引用、重复 ID、提示注入、非法 JSON、异常、超时和安全回退测试通过 |
| D4 | Digest Schema 1.0、历史/latest Markdown 原子提交及失败恢复测试通过 |
| D5 | 中文 digest help、collect.sh 默认兼容与显式 `--digest` 测试通过 |
| D6 | 文档检查、diff check、ruff、mypy、全量测试通过 |

实现完成前状态最高为 `Implemented`；本计划不替代任何真实来源、真实 Codex 或真实定时验收。

## 离线实现验收记录（2026-08-09）

- `python3 scripts/check_docs.py`：27 份 Markdown，0 errors。
- `git diff --check`：通过。
- `UV_CACHE_DIR=/tmp/mynews-v13-uv-cache uv run ruff check .`：通过。
- `UV_CACHE_DIR=/tmp/mynews-v13-uv-cache uv run mypy src`：37 个源文件，0 问题。
- `UV_CACHE_DIR=/tmp/mynews-v13-effort-uv-cache uv run pytest -q`：183 passed。
- `uv run mynews --help`、`collect/probe/validate/report/digest --help` 和 `scripts/collect.sh --help`：全部退出码 0，帮助为中文并包含核验/摘要推理强度参数。
- 未调用真实 launchd，未向仓库 `output/`、`state/`、`logs/` 写入或提交运行数据。

因此离线实现为 `Implemented`；真实 G6-V 复验见下节，因没有 verified 条目仍不写成 `Verified`。

## v1.3 真实复验记录（2026-08-09）

- 在仓库外隔离目录使用模型 `gpt-5.6-luna`、`medium`、单条批次和 300 秒超时执行两轮同一七天窗口（2026-08-02 至 2026-08-08）。两轮均真实访问来源并生成 `requested_range.verification_reasoning_effort=medium`。
- 首轮 Run ID `2026-08-09T06:43:00.860910+00:00`：`partial`，2 个 discovery 条目，`verified_count=0`；原因包含 `codex_no_suggestion` 和 `candidate_redirect_anomaly`，另有真实 `cc-switch` 403。`validate --check-evidence` 的 Schema 校验通过，但 `evidence_count=0`、`evidence_checked=false`。生产 Digest 写入历史和 latest，主榜 0、线索 2。
- 次轮 Run ID `2026-08-09T06:45:37.378610+00:00`：`partial`，`deduplicated_count=2`、`retried=1`、`pending=1`、`verified_count=0`。第二次 Digest 后历史文件数为 2，latest 完整，条目生命周期为 `ongoing`，无 `.tmp` 文件。
- 使用合成的 verified 条目并将 `codex` 替换为返回失败的隔离可执行文件，模拟 Codex 不可用：Digest 生成合法 `partial`，主榜条目仍为 `verified`，摘要为 `fallback`，证据引用保留且无临时文件。该模拟只证明安全回退，不计入 G6-V。

因此本次真实验收结论为 `BLOCKED`：真实网络和 Codex 均已执行，但尚未产生至少一条同时满足 `verified`、第一方证据复核和 Codex 摘要的主榜故事；不得以 mock 或无证据条目代替。
