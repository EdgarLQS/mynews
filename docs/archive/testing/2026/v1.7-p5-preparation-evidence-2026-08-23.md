---
title: mynews v1.7 P5 历史准备证据 2026-08-23
doc_type: test
status: archived
implementation_status: in_progress
version: 1.0
created: 2026-08-23
updated: 2026-08-23
owner: project-maintainers
superseded_by: ../../../planning/v1.7-v2.0-master-plan.md
---

# mynews v1.7 P5 历史准备证据 2026-08-23

## 定位与结论

本文保存统一总计划建立前已经产生的 P5 准备证据。它只说明插件分发、逐源 probe、选择模式
和一次手工彩排曾经运行，不是当前 P5 验收记录，不能把 AI-02、DATA-07 或 OPS-04 上调为
Verified。

结论：**历史准备部分完成；P5 未 PASS。**

## 固定环境

| worktree | 固定提交 | Git 状态 | 用途 |
| --- | --- | --- | --- |
| `/private/tmp/mynews-p5-runtime` | `e60c0c7bb537b20c434ca6da155eb29b1301447f` | detached / clean | 插件构建、安装、逐源 probe 和扩展采集准备 |
| `/private/tmp/mynews-p5-runtime-fix` | `003db290a71000bd00d8aeb65efe96d4c8cf8869` | detached / clean | prepare 修复后的选择模式和 18:00 手工彩排 |

两个目录都是仓库外临时运行目录，未提交其中的 `output/`、`state/`、`logs/` 或 wheel。
旧目录和日志在新的 P5 证据完成前保留，不自动删除。

## 已取得的准备证据

### 插件分发与逐源 probe

- 构建包：`mynews-newsfromai-sources==0.1.0`；
- wheel SHA-256：`d370ec351263b4eee9f2f46884263191a2bf20458425a40c1dabbe613537211d`；
- 分发元数据发现 15/15 entry-point，插件包测试 47 passed；
- 15/15 插件 probe 均执行并退出 0：7 个 primary healthy、4 个 incident healthy、
  2 个 research healthy 且合法空结果、1 个 discovery healthy、1 个 benchmark/manual
  按预期为 `manual_source` blocked；
- 逐源 probe 合计抓取 60 条、接受 60 条；`plugin list loaded=false` 只代表未执行工厂，
  不被解释成插件已加载；
- 默认 built-in、plugin-only 和 `--with-plugin` 追加模式已有运行产物；扩展采集保留 27 个
  source_id，latest 与历史 run 哈希一致。

### 离线与手工彩排

- 旧固定提交上的全量测试记录为 291 passed；
- 扩展采集结果为 complete，但 `verified_count=0`、`unverified_count=26`，没有可用于 G6-V 的
  verified 样本；
- `003db29` worktree 生成过 `2026-08-23-1800.md`、Digest 和 automation state；状态记录
  `lastCompletedSlot=2026-08-23 18:00`，Digest 为 0 个 main item、20 个 lead item；
- 上述 18:00 结果来自手工彩排，不是 Scheduled Task 自动档位证据。

## 不能复用为 PASS 的限制

1. 两个 worktree 都在 `src/mynews/**/__pycache__` 留下运行残留，超出允许写入的
   `output/`、`state/`、`logs/` 边界；新 P5 必须使用全新 worktree，并把
   `PYTHONPYCACHEPREFIX` 等缓存固定到 `logs/p5/runtime/`。
2. 没有已注册的 mynews Scheduled Task，也没有真实 09:00 与 18:00 两档自动运行证据。
3. 没有跨过至少两个档位后的 `latest_only`、前置失败保护、状态目录写入阻断和恢复运行证据。
4. 没有完成真实 G6-V；0 个 verified item 不能证明生产 Codex 与程序二次校验成功。
5. P5 固定代码已从 `e60c0c7` 变化到 `003db29`，且 `prepare.py` 属于五步任务链；后续必须从
   包含统一总计划的最新 `main` 重新冻结，不沿用旧固定提交作为最终结论。

因此旧结果最高只能作为准备线索。后续顺序和授权边界以
[统一开发总计划](../../../planning/v1.7-v2.0-master-plan.md) 的 M1 为准。
